# Dispute Route

HTTP endpoint to initiate a dispute on a deal.

- Type: Route (legacy stub)
- Location: `actions-server/src/routes/dispute/index.ts`
- Method: `POST /api/escrow/dispute`

## Input

`RefundActionInput` (placeholder) — see `actions-server/src/types/actions.ts`

## Output

Currently throws `Dispute handling moved to /actions endpoints`.

### Updates

- v1.0.0 — Initial creation
- v1.2.0 — Placeholder pending `/actions/open-dispute`
